/**
 * wp-native-theme — design token primitives for wp-native.
 *
 * Implementation lands in M5. This file currently exports a placeholder so
 * the TypeScript project graph compiles cleanly across the monorepo.
 */

export const PACKAGE_NAME = 'wp-native-theme' as const;
