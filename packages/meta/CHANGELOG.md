# Changelog

## 0.0.1 — 2026-05-02

Initial v0.0.1 stub published to claim the `wp-native` npm name. This package is intentionally a no-install meta package — it throws on import with a redirect to the real packages (`wp-native-client` and `wp-native-shell`).
